<%= form_for(@monster, :html => { :multipart => true } ) do |f| %>
  <% if @monster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monster.errors.count, "error") %> prohibited this monster from being saved:</h2>

      <ul>
      <% @monster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, :placeholder => "Name" %>
  </div>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.text_area :description, :placeholder => "Description", :columns => 20, :rows => 10 %>
  </div>
  <div class="actions">
    <%= f.submit "Send", :class => "btn" %>
  </div>
<% end %>
